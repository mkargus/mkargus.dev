import { Request, Response, NextFunction } from 'express'

export const getContactIndex = (req: Request, res: Response, next: NextFunction): void => {
  next()
}

export const postContact = (req: Request, res: Response, next: NextFunction): void => {
  next()
}
